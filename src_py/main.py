from vision import video_stream,camera,demo
from web_debugger.app import app

if __name__ == '__main__':
    #video_stream.video_show()
    #camera.cam_show()
    demo.demo_show()
    # app.cam_index = 0
    # app.run(host="0.0.0.0", debug=True, port=5000)

